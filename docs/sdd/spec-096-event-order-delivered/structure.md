# Estructura — SPEC-096

```text
ordering.order-item.delivered.v1
├── envelope SPEC-217
├── order/item/allocation references
├── actorType + deliveryChannel
└── deliveredAt + revisions

ordering.order.delivered.v1
├── envelope SPEC-217
├── order-level scope
└── derived DELIVERED transition metadata
```
