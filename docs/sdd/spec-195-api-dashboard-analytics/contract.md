# Contrato API — SPEC-195 Analytics Dashboards

Crear, versionar, publicar y consultar dashboards con filtros server-side y caché tenant-aware.
Cada widget devuelve freshness, cobertura y error aislado para degradación parcial. Tests
cubren permisos heterogéneos, timeout, métricas retiradas, filtros, caché, accesibilidad,
concurrencia y aislamiento.
