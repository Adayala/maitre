# Customer frontend — environment.
# Public surfaces work without auth. Set the Supabase vars for real customer
# sign-in; leave them empty only for a local fixture-login fallback backend.
VITE_API_URL=http://localhost:3001
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
