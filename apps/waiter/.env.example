# Waiter (mozo) frontend — environment.
# Point this at the Maitre API. Set the Supabase vars for real auth; leave them
# empty only for a local fixture-login fallback backend.
VITE_API_URL=http://localhost:3001
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
