# Verificación — SPEC-204

## Criterios

### CAD-204-01 — ActionRegistry cubre IDs, tiers, inputs, permisos, límites, adapters y auditoría

ActionRegistry cubre IDs, tiers, inputs, permisos, límites, adapters y auditoría.

### CAD-204-02 — Tiers 0/1/2/3 quedan definidos con prohibición explícita de Tier 3

Tiers 0/1/2/3 quedan definidos con prohibición explícita de Tier 3.

### CAD-204-03 — I0 mantiene execution `OFF` y sólo ofrece sugerencias o previews

I0 mantiene execution `OFF` y sólo ofrece sugerencias/previews.

### CAD-204-04 — Insights o predictions desactualizados y text injection no crean commands ejecutables

Insights/predictions desactualizados o text injection no crean commands ejecutables.

### CAD-204-05 — Kill switch, budget, rate/concurrency limits y reconciliation existen

Kill switch, budget, rate/concurrency limits y reconciliation existen.

### CAD-204-06 — Fixtures cubren tiers, suggestion-only, bloqueo por desactualización, kill switch y reconciliation

Fixtures cubren tiers, suggestion-only, bloqueo por desactualización, kill switch y reconciliation.
