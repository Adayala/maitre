# Estructura — SPEC-120

```text
workforce.work-shift.completed.v1
├── envelope SPEC-217
├── scope: tenantId, brandId, branchId, workShiftId
├── completedAt + outcome
├── laborPolicyVersion + aggregateRevision
└── privacy-safe completion flags/aggregates
```
