# Plan — SPEC-022

- Congelar response schema y envelope de `GET /v1/roles`.
- Definir filtros, orden, ETag y conditional GET.
- Definir redacción de permissions internas y cálculo de `assignable`.
- Preparar casos OWNER/ADMIN/MANAGER, deprecated, cache y cross-tenant.

La estimación permanece pendiente hasta aprobar schema y matriz de autorización.
