# Especificación — SPEC-'"$num"'

## Endpoints

- GET /'"${name}"' (list)
- POST /'"${name}"' (create)
- GET /'"${name}"'/:id (detail)
- PATCH /'"${name}"'/:id (update)
- DELETE /'"${name}"'/:id (archive)
