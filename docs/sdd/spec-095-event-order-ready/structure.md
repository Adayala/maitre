# Estructura — SPEC-095

```text
ordering.order-item.ready.v1
├── envelope SPEC-217
├── order/item/allocation/station references
└── readyAt + revisions

ordering.order.ready.v1
├── envelope SPEC-217
├── order-level scope and aggregateRevision
└── derived READY transition metadata
```
