# Estructura — SPEC-172

```text
Integration
├── tenant / provider / environment
├── adapter version / capabilities
├── non-secret config + secret refs
├── ownership matrix version
├── lifecycle status
└── audit revision / history
```
