# Host / maître frontend — environment.
# Point this at the Maitre API. Leave the Supabase vars empty to use fixture
# login (e.g. the demo-token on a local API with AUTH_DRIVER=fixture).
VITE_API_URL=http://localhost:3001
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
