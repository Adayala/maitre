# Estructura — SPEC-093

```text
Special Requests API
├── create request against Reservation | Visit | Order
├── validate type policy + consent/retention
├── store typed metadata + optional sanitized text
├── accept/reject by authorized operator
└── fulfill after accepted operational handling
```
