# Customer frontend — environment.
# Public surfaces work without auth; leave Supabase vars empty to use the local
# fixture login for customer reservation testing in dev.
VITE_API_URL=http://localhost:3001
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
