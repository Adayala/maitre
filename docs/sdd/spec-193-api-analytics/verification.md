# Verificación — SPEC-193

## Criterios

### CAD-193-01 — Ingest valida identity, firma, schema, size/rate, clock y dedupe

Ingest valida identity, firma, schema, size/rate, clock y dedupe.

### CAD-193-02 — La respuesta por item usa `ACCEPTED|DUPLICATE|QUARANTINED|REJECTED`

Respuesta por item usa `ACCEPTED|DUPLICATE|QUARANTINED|REJECTED`.

### CAD-193-03 — Tenant y subject no se confían al payload público

Tenant/subject no se confían al payload público.

### CAD-193-04 — Query usa métricas publicadas con límites de rango, costo y cardinalidad

Query usa métricas publicadas con límites de rango/costo/cardinalidad.

### CAD-193-05 — Respuestas publican timezone, cursor, `asOf`, freshness, coverage y suppression

Respuestas publican timezone, cursor, `asOf`, freshness, coverage y suppression.

### CAD-193-06 — Fixtures cubren ingest, quarantine, trust, límites y supresión

Fixtures cubren ingest, quarantine, trust, límites y supresión.
