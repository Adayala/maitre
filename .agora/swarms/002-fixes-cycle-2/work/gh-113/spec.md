# Spec: error de capacidad localizado

Los conflictos por falta de mesas usan código `RESERVATION_CAPACITY_UNAVAILABLE`, título y detalle accionables en español. El cliente Host prioriza `detail` del contrato Problem Details y conserva fallbacks para otras respuestas.
