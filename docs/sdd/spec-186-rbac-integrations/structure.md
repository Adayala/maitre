# Estructura — SPEC-186

```text
Integrations RBAC
├── integration read / configure / activate / disable
├── oauth authorize / revoke / credential rotate
├── sync run / retry
├── webhook outbound manage / inbound manage
├── integration test
└── integration audit read
```
