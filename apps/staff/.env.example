# Staff shell frontend — environment.
# This app mainly launches role apps, so it doesn't need API credentials for
# the first iteration.
VITE_API_URL=http://localhost:3001
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
