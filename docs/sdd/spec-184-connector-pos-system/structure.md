# Estructura — SPEC-184

```text
POS Connector
├── ownership matrix by resource/field
├── external id mapping
├── echo suppression
├── archive / tombstone semantics
└── version / checkpoint convergence
```
