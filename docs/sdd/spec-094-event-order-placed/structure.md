# Estructura — SPEC-094

```text
ordering.order.submitted.v1
├── SPEC-217 envelope
├── scope: tenantId, brandId, branchId, visitId, orderId
├── revisions: catalogRevisionId, aggregateRevision
├── commercial summary: currency, subtotal, taxTotal, grandTotal
└── submittedAt + correlation metadata
```
