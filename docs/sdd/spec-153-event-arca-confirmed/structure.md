# Estructura — SPEC-153

```text
fiscal.authorization.resolved.v1
├── envelope SPEC-217
├── operation / invoice refs
├── provider / environment
├── outcome / normalized codes
├── resolvedAt / causation
└── no raw SOAP / no credentials / no PII
```
