# Cashier (caja) frontend — environment.
# Point this at the Maitre API. Leave the Supabase vars empty to use the
# fixture-token login (AUTH_DRIVER=fixture backend, e.g. the demo token).
VITE_API_URL=http://localhost:3001
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
